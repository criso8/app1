<%= form_for(@work_out) do |f| %>
	<div class="field">
		<p>date</p>
		<%= f.text_field :date%>
	</div>
	<div class="field">
		<p>meters</p>
		<%= f.text_field :meters%>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>