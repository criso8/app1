table>
<tr>
<td >title</td>
<td >user</td>
</tr>
<% @task_lists.each do |task_list| %>
<tr>

<td><%= task_list.title %></td>
<td><%= (task_list.user).name %></td>
<%= link_to “show task_lists”, task_lists_path(task_lists.id) %>
</tr>
<% end %>
</table>